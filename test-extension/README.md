# Hublead Placeholder Test Extension

This is the simplest loadable Chrome extension for checking where the real Hublead panel should appear.

Build it with:

```sh
npm run test-extension:build
```

Then load this folder in Chrome:

```text
test-extension/build
```

It runs on LinkedIn company pages and displays a fixed placeholder in the same position as the actual extension panel.
